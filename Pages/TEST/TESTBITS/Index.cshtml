@page
@model Pet_Reunion_Hub.Pages.TEST.TESTBITS.IndexModel

@{
    ViewData["Title"] = "Index";
    // Layout = "~/Pages/Shared/SignUp.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TESTBIT[0].Hello)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TESTBIT[0].Cute)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TESTBIT[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TESTBIT) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hello)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cute)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
