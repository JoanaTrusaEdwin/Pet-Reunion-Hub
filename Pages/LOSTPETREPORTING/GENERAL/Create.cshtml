@page
@model Pet_Reunion_Hub.Pages.LOSTPETREPORTING.GENERAL_LOCATION.CreateModel

@{
    ViewData["Title"] = "Create";
    // Layout = "~/Pages/Shared/SignUp.cshtml";
}

<h1>Create</h1>

<h4>GENERALLOCATION</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GENERALLOCATION.LOCATIONVALUE" class="control-label"></label>
                <input asp-for="GENERALLOCATION.LOCATIONVALUE" class="form-control" />
                <span asp-validation-for="GENERALLOCATION.LOCATIONVALUE" class="text-danger"></span>
            </div>
            @* <div class="form-group">
                <label asp-for="GENERALLOCATION.UserId" class="control-label"></label>
                <select asp-for="GENERALLOCATION.UserId" class ="form-control" asp-items="ViewBag.UserId"></select>
            </div> *@
            <input type="hidden" asp-for="GENERALLOCATION.UserId" />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
