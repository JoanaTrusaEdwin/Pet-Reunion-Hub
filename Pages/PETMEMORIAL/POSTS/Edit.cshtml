@page
@model Pet_Reunion_Hub.Pages.PETMEMORIAL.POSTS.EditModel

@{
    ViewData["Title"] = "Edit";
    // Layout = "~/Pages/Shared/SignUp.cshtml";
}

<h1>Edit</h1>

<h4>Post</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Post.Id" />
            <div class="form-group">
                <label asp-for="Post.Content" class="control-label"></label>
                <input asp-for="Post.Content" class="form-control" />
                <span asp-validation-for="Post.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Post.IsPublic" class="control-label"></label>
                <input asp-for="Post.IsPublic" class="form-control" />
                <span asp-validation-for="Post.IsPublic" class="text-danger"></span>
            </div>
           @*  <div class="form-group">
                <label asp-for="Post.TributeId" class="control-label"></label>
                <select asp-for="Post.TributeId" class="form-control" asp-items="ViewBag.TributeId"></select>
                <span asp-validation-for="Post.TributeId" class="text-danger"></span>
            </div> *@
            @* <div class="form-group">
                <label asp-for="Post.UserId" class="control-label"></label>
                <select asp-for="Post.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="Post.UserId" class="text-danger"></span>
            </div> *@

            <input type="hidden" asp-for="Post.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>

        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
