@page
@model Pet_Reunion_Hub.Pages.PETMEMORIAL.POSTS.DetailsModel

@{
    ViewData["Title"] = "Details";
    // Layout = "~/Pages/Shared/SignUp.cshtml";
}
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Mvc.RazorPages

<style>
    /* Style for Edit and Back to List buttons */

    #edit {
        border-radius: 5px;
        background-color: #8b572a;
        color: white;
        text-decoration: none;
        padding: 5px 10px;
        font-weight: bold;
        transition: background-color 0.3s;
    }

    #back {
        border-radius: 5px;
        background-color: black;
        color: white;
        text-decoration: none;
        padding: 5px 10px;
        font-weight: bold;
        transition: background-color 0.3s;
    }

    .btn:hover {
        background-color: #555; /* Darken the background color on hover */
    }
</style>




<div style="padding: 30px; margin-bottom: 20px; border: 1px solid #ccc; border-radius: 5px; background-color: #fff;">
    <h4>Your Memory</h4>
    <hr />
   @*  <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Post.Content)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Post.Content)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Post.IsPublic)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Post.IsPublic)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Post.User)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Post.User.Id)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Post.Tribute)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Post.Tribute.UserId)
        </dd>
    </dl> *@
    <dl>
        <dt>Created on:</dt>
        <dd>@Model.Post.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</dd>

        <dt>Caption:</dt>
        <dd>@Model.Post.Content</dd>


        <dt>Visibility:</dt>
        <dd>@Model.Post.IsPublic</dd>

 
        <div>
            <h4>Your Media Files</h4>
            @if (Model.Post.Media != null && Model.Post.Media.Any())
            {
                <div>
                    @foreach (var file in Model.Post.Media)
                    {
                        <img src="@file.MediaUrl" alt="Additional File" style="max-width: 300px;" />
                        
                    }
                </div>
            }
            else
            {
                <p>No additional files available.</p>
            }
        </div>
</dl>

<div>
    <a asp-page="./Edit" asp-route-id="@Model.Post?.Id" id="edit">Edit</a> 
    <a asp-page="./Index" id="back">Back to List</a>
</div>
</div>
