@page
@model Pet_Reunion_Hub.Pages.COPINGRESOURCES.NEW.IndexModel

@{
    ViewData["Title"] = "Index";
    // Layout = "~/Pages/Shared/SignUp.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].TITLE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].DESCRIPTION)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].LINK)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].FILEURL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].TYPE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RESOURCE[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RESOURCE) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TITLE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DESCRIPTION)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LINK)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FILEURL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TYPE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
